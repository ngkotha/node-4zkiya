// run `node index.js` in the terminal

let age = 28;

function voting(){
  if (age >= 18){
    console.log('elogible for voting');
  } else {
    console.log('not eligible for voting');
  }
}
voting();

console.log(`My first Node program`);
